{
  "name": "lifehack2026",
  "version": "0.1.0",
  "private": true,
  "description": "A social sustainability challenge web application.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint:styles": "stylelint \"src/**/*.scss\"",
    "test": "node --test && npm run lint:styles && npm run build"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.13.1"
  },
  "devDependencies": {
    "sass": "^1.103.1",
    "stylelint": "^17.14.1",
    "stylelint-config-standard-scss": "^17.0.0",
    "vite": "^8.2.2"
  }
}
